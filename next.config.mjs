// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com"],
  },
};

// Cambia esto para usar la sintaxis de módulos ES
export default nextConfig;
